This is to ensure the glue between federated use management (FEUDAL) and
the LDAP Facade (LDF, Web-Registry) works fine.

To enable a service this might be helpful:
```
systemctl --user --now enable feudalClient@0
```

M.
